/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAnalyticsPda } from "../pdas";
import { ANCHOR_AMM_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_ANALYTICS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([39, 6, 0, 253, 143, 16, 81, 194]);

export function getInitializeAnalyticsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_ANALYTICS_DISCRIMINATOR,
  );
}

export type InitializeAnalyticsInstruction<
  TProgram extends string = typeof ANCHOR_AMM_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountAnalytics extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAnalytics extends string
        ? WritableAccount<TAccountAnalytics>
        : TAccountAnalytics,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeAnalyticsInstructionData = {
  discriminator: ReadonlyUint8Array;
  admin: Address;
};

export type InitializeAnalyticsInstructionDataArgs = { admin: Address };

export function getInitializeAnalyticsInstructionDataEncoder(): FixedSizeEncoder<InitializeAnalyticsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_ANALYTICS_DISCRIMINATOR,
    }),
  );
}

export function getInitializeAnalyticsInstructionDataDecoder(): FixedSizeDecoder<InitializeAnalyticsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
  ]);
}

export function getInitializeAnalyticsInstructionDataCodec(): FixedSizeCodec<
  InitializeAnalyticsInstructionDataArgs,
  InitializeAnalyticsInstructionData
> {
  return combineCodec(
    getInitializeAnalyticsInstructionDataEncoder(),
    getInitializeAnalyticsInstructionDataDecoder(),
  );
}

export type InitializeAnalyticsAsyncInput<
  TAccountPayer extends string = string,
  TAccountAnalytics extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  analytics?: Address<TAccountAnalytics>;
  systemProgram?: Address<TAccountSystemProgram>;
  admin: InitializeAnalyticsInstructionDataArgs["admin"];
};

export async function getInitializeAnalyticsInstructionAsync<
  TAccountPayer extends string,
  TAccountAnalytics extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANCHOR_AMM_PROGRAM_ADDRESS,
>(
  input: InitializeAnalyticsAsyncInput<
    TAccountPayer,
    TAccountAnalytics,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeAnalyticsInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAnalytics,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANCHOR_AMM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    analytics: { value: input.analytics ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.analytics.value) {
    accounts.analytics.value = await findAnalyticsPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("analytics", accounts.analytics),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeAnalyticsInstructionDataEncoder().encode(
      args as InitializeAnalyticsInstructionDataArgs,
    ),
    programAddress,
  } as InitializeAnalyticsInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAnalytics,
    TAccountSystemProgram
  >);
}

export type InitializeAnalyticsInput<
  TAccountPayer extends string = string,
  TAccountAnalytics extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  analytics: Address<TAccountAnalytics>;
  systemProgram?: Address<TAccountSystemProgram>;
  admin: InitializeAnalyticsInstructionDataArgs["admin"];
};

export function getInitializeAnalyticsInstruction<
  TAccountPayer extends string,
  TAccountAnalytics extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ANCHOR_AMM_PROGRAM_ADDRESS,
>(
  input: InitializeAnalyticsInput<
    TAccountPayer,
    TAccountAnalytics,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeAnalyticsInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountAnalytics,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ANCHOR_AMM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    analytics: { value: input.analytics ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("analytics", accounts.analytics),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeAnalyticsInstructionDataEncoder().encode(
      args as InitializeAnalyticsInstructionDataArgs,
    ),
    programAddress,
  } as InitializeAnalyticsInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAnalytics,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeAnalyticsInstruction<
  TProgram extends string = typeof ANCHOR_AMM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    analytics: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeAnalyticsInstructionData;
};

export function parseInitializeAnalyticsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeAnalyticsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      analytics: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeAnalyticsInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
